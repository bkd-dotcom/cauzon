/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // PWA: served with a manifest + service worker registered client-side.
  // Kept dependency-free (no next-pwa) so the scaffold installs fast.
};

module.exports = nextConfig;
