# Incident Root-Cause Analysis — weekly_sales revenue is reporting $0 for all weeks

**Detected:** today 09:12  
**Symptom asset:** `urn:li:dataset:(urn:li:dataPlatform:snowflake,shop.weekly_sales,PROD)`  
**Failed assertion:** weekly_sales.revenue must be > 0

## Root cause (grounded)
**raw_orders** — `urn:li:dataset:(urn:li:dataPlatform:postgres,shop.raw_orders,PROD)`  
Confidence: 70%

### Evidence
- Column `amount` renamed to `order_amount` 6 hours ago.

### Verifiable lineage proof path
```
shop.raw_orders -> shop.orders_enriched -> shop.weekly_sales
```

**Transform that carried the fault downstream:**
```sql
SELECT order_id, customer_id, amount AS revenue, created_at FROM raw_orders
```

## Recommended fix
Reconcile the schema change on `raw_orders` with downstream consumers; update the transform SQL and add a schema contract.

---
_Generated by Cauzon — path-grounded RCA agent for DataHub._
