<!-- Generated by scripts/build_examples.py — real Cauzon output, not hand-written. Do not edit by hand. -->

# Incident Root-Cause Analysis — daily_revenue is 40% below expected volume

**Detected:** today 08:04  
**Symptom asset:** `urn:li:dataset:(urn:li:dataPlatform:snowflake,nyc.daily_revenue,PROD)`  
**Failed assertion:** row_count within 10% of 7-day average  
**Grounding:** Path + transform proven

## Root cause
**raw_trips** — `urn:li:dataset:(urn:li:dataPlatform:s3,nyc.raw_trips,PROD)`  
Confidence: 85%
Owner: data-platform@example.com

### Evidence
- Freshness 51h exceeds the 24h SLA (+27h stale).
- Row count changed -100% vs baseline.
- No upstream shows this fault — the fault appears to originate here.

### How this confidence was derived
- Grounding ×1.0 — Lineage path and the transform that carried the fault were both reconstructed.
- Signals ×0.85 — 2 independent signal(s) agree: freshness lag, volume anomaly.
- Origin ×1.0 — No upstream asset shows this fault, so it originates here.

### Recurrence
This asset has 2 prior Cauzon dossier(s):
- [Cauzon] RCA: raw_trips ingestion stalled (daily_revenue volume drop) (`urn:li:document:(cauzon,rca-raw_trips-ingestion-stalled-2026-07-22)`)
- [Cauzon] RCA: raw_trips ingestion stalled (daily_revenue volume drop) (`urn:li:document:(cauzon,rca-raw_trips-ingestion-stalled-2026-08-01)`)

### Verifiable lineage proof path
```
nyc.raw_trips -> nyc.trips_cleaned -> nyc.daily_revenue
```

**Transform that carried the fault downstream:**
```sql
CREATE OR REPLACE TABLE trips_cleaned AS SELECT * FROM raw_trips WHERE fare_amount > 0
```

### Candidates rejected by the proof gate
- **marketing_spend** (score 8.0) — No lineage path connects this asset to the symptom, so the claim cannot be grounded.

## Recommended fix
Restart the ingestion job feeding `raw_trips` and backfill the missing window; it is the upstream origin of the staleness. Owner: data-platform@example.com. This is failure #3 for this asset — treat the schedule or contract as the real defect, not just this occurrence.

```bash
# 1. backfill the stalled source
datahub ingest -c raw_trips.yml
# 2. replay downstream, in dependency order
#    nyc.trips_cleaned → nyc.daily_revenue
```

Downstream assets only recover once fresh data lands, so replay them in lineage order after the backfill completes.

## Summary
`raw_trips` is the origin of this incident, and the fault reached the symptom along nyc.raw_trips → nyc.trips_cleaned → nyc.daily_revenue. Path + transform proven at 85% confidence. Evidence: Freshness 51h exceeds the 24h SLA (+27h stale). `marketing_spend` scored higher on signals alone but was rejected: No lineage path connects this asset to the symptom, so the claim cannot be grounded. This asset has failed 2 time(s) before.

---
_Generated by Cauzon — path-grounded RCA agent for DataHub._
